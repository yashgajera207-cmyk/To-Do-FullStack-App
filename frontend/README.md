# TaskFlow Frontend

Next.js 15 (App Router) + TypeScript + Tailwind CSS frontend for the Task Manager backend.

## Setup

```bash
npm install
npm run dev
```

Configure `.env.local`:
```
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
```

Make sure the backend (Express + Prisma) is running on port 5000.
